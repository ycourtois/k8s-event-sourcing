apiVersion: v1
kind: Service
metadata:
  name: hystrix-dashboard
spec:
  selector:
    app: hystrix-dashboard
  ports:
    - protocol: TCP
      port: 6161
      targetPort: 6161
      nodePort: 31161
  type: NodePort