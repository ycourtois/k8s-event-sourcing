apiVersion: v1
kind: Service
metadata:
  name: neo4j-service
spec:
  selector:
    app: neo4j-service
  ports:
    - protocol: TCP
      port: 7474
      targetPort: 7474
      nodePort: 31474
  type: NodePort